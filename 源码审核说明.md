# 牛马邮箱 v0.91.0 源码审核包

本压缩包用于第三方源码审核，包含当前工作区完整可运行源码、依赖清单、构建脚本和自动化测试。

## 目录说明

- `ophelia_assistant/`：核心业务、浏览器自动化、数据库、诊断和 PySide6 Studio 界面
- `tests/`：自动化测试
- `assets/`：图标与界面素材
- `installer/`：安装器辅助脚本
- `requirements.txt` / `requirements-lock.txt`：Python 依赖
- `RUN_SOURCE.bat`：源码运行入口
- `BUILD_WINDOWS_EXE.bat`：Windows 便携版构建脚本
- `NiuMaMail-v0.91.0.spec`：PyInstaller 构建配置
- `牛马邮箱v0.91.0使用手册.docx`：用户使用手册

## 运行与测试

```powershell
python -m venv .venv
.\.venv\Scripts\pip install -r requirements-lock.txt
.\.venv\Scripts\python -m unittest discover -s tests -p "test_*.py" -v
```

当前自动化测试结果：187 个测试全部通过，0 失败，0 跳过。

## 本轮主要改动

- 窗口顺序与窗口绑定改为独立业务保存，不再重写全部设置，也不会重新加密 AdsPower API Key
- “保存窗口绑定”改为合并保存，只更新当前窗口，历史绑定保留；只有“清理无效绑定”才永久删除
- 增删、改号、移动窗口时会先按窗口编号读取当前绑定表，刷新后恢复未保存的模板、发件人和锁定状态
- 保存流程改为“先原子写盘、成功后再更新内存”，写盘失败时窗口顺序、绑定和历史数据都保持最后成功保存的状态
- 局部保存只发布本次提交的字段，不覆盖内存中尚未保存的 API Key，也不清除 `_api_key_dirty`
- 自动发送改用 Gmail 提示节点 ID 判断新发送结果，旧提示残留不会误阻断发送
- 窗口绑定表严格只显示当前 `window_sequence` 中的窗口；自动填充、增删、改号、上下移、保存后立即同步
- 历史绑定按窗口编号保留，重新打开窗口后可恢复，但已关闭窗口不参与邮件分配
- 设置文件增加 `schema_version` / `app_version` / `updated_at` / `migration_id`，旧配置只迁移一次并自动备份
- 设置保存使用临时文件 + JSON 校验 + `os.replace()` 原子替换；单个字段损坏只恢复该字段
- 授权状态改为唯一主数据目录；旧目录仅在主文件缺失时一次性迁移，并写迁移日志
- 授权异常保护：检测到无法解释的授权时长变化时保留原文件、写诊断日志并提示核对，不再自动写回
- 设置页新增“数据与授权诊断”，可查看路径/版本/来源，并支持打开数据目录、导出脱敏诊断、检查一致性
- 每封邮件记录获取窗口、CDP 连接、打开 Gmail、打开 Compose、填写、校验、点击 Send、等待结果等阶段耗时
- 本地邮件批量渲染并单事务写入数据库，100 封不再逐封提交
- 同一浏览器窗口在一次批量任务中复用 CDP 连接，窗口间并行、窗口内串行
- Gmail 收件人校验兼容输入框、联系人标签、`data-hovercard-id`、`aria-label`、`data-email` 等读取来源
- 校验失败禁止点击 Send，可安全重试；只有点击 Send 后结果不明确才进入人工确认
- 窗口绑定模板下拉框支持模板库变更后自动刷新，并保留窗口当前选择
- 自动获取浏览器窗口时保留已保存的自定义顺序，新窗口追加到末尾
- 右侧联系人资料/邮件预览/当前操作改为可折叠详情面板，宽度和展开状态持久化
- 详情面板展开后可真实拖动调整宽度，不再被固定宽度锁死
- 详情栏状态写入独立 `ui_state.json`，不再保存整个业务设置文件
- DPAPI 的 `LocalFree` 改由 `kernel32` 调用，并补充错误码日志与原子写入
- AdsPower API Key 仅在设置页修改时重新加密，已加密值不会二次加密
- 单次生成上限为 100 封，数据库仍可长期保存 300 封以上
- 保留退出时的“后台运行 / 直接退出 / 取消”三种选择

## 审核重点

1. 发送安全：`send_clicked_at` 存在时禁止自动重发
2. Gmail 校验：收件人转标签后仍能正确读取
3. 浏览器连接：不得关闭用户已打开的 BitBrowser / MoreLogin / AdsPower 窗口
4. 任务恢复：崩溃后 `sending + send_clicked_at` 恢复为 `needs_review`
5. 数据持久化：窗口顺序、模板绑定、发件人、锁定状态和详情栏状态
