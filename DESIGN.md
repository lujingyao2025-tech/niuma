# 牛马邮箱 · 外贸邮件工作室

---

## Overview

**Creative North Star: "外贸邮件工作室"**

牛马邮箱把批量发信从"任务调度台"重构为一张连续的邮件工作室桌面：56px 功能轨固定在最左，活动/批次列表承载客户分组，中央工作区把联系人、内容、窗口和草稿放在同一上下文，右侧检查器始终交代当前联系人资料、邮件预览与正在执行的操作。参考 Linear 的视觉降噪与导航密度、Attio 的连续工作空间、Superhuman 的键盘优先与命令式操作，以及 Windows 桌面应用对原生窗口、输入和反馈的预期。

**Key Characteristics:**

- 默认克制浅色专业界面：冷灰工作台、白色面板、1px 细线和单一蓝色主动作。
- 活动/批次是产品一级对象：每个活动独立管理联系人、模板、窗口和草稿。
- 中央是高密度、可编辑的任务表，右侧常驻上下文检查器。
- 全局搜索/命令面板（Ctrl+K）与清晰键盘操作覆盖所有核心视图。
- 状态同时用颜色、文字和数量表达，不依赖颜色单独传达含义。

## Colors

主色是克制的信号蓝，只用于选择、当前导航和主动作。界面以冷灰白为基底：工作台 `#F3F5F7`、面板与纸面 `#FFFFFF`、正文 `#1C2430`、次要文字 `#5B6775`、分隔线 `#E2E7EC`。完成绿、提醒琥珀、草稿蓝与危险红只表达既定语义；深色主题保持同一角色映射。

**The Restrained Palette Rule.** 大面积深海军蓝、玻璃拟态、Bento Grid 和指标卡堆叠均不使用；蓝色只点亮必要动作。

## Typography

正文与操作统一使用 Microsoft YaHei UI / Segoe UI，层级靠粗细和有限字号建立。页面标题 16px 加粗，分区标题 13px 加粗，正文 13px，辅助与表格 12px；紧凑但可读，适配 Windows 高 DPI。

**The Single Face Rule.** 一个专业无衬线字族承载全部界面；展示字体不进入标签、按钮和数据。

## Layout

```
┌─ 56px 功能轨 ┬─ 活动/批次列表 ┬─ 当前活动工作区 ────────┬─ 详情检查器 ─┐
│ 活动          │ Seattle 客户     │ 联系人 / 内容 / 窗口 / 草稿   │ 联系人资料    │
│ 联系人        │ 德国经销商       │ 高密度、可编辑任务表           │ 邮件预览      │
│ 模板          │ 八月跟进         │                                │ 当前操作      │
│ 历史          │                  │                                │               │
└───────────────┴─────────────────┴────────────────────────────────┴───────────────┘
```

56px 功能轨只承载图标与当前页标记；活动/批次列表约 248px；中央工作区占主要宽度，并以联系人/内容/窗口/草稿四个页签保持上下文连续；右侧检查器 300–420px 常驻。空间不足时保持表格可用并允许滚动，不把桌面工作台伪装成网页卡片流。

**The Studio Continuity Rule.** 联系人在哪里被选中，右侧检查器就在哪里同步；当前活动、当前选中和下一步操作始终可见。

## Components

- **56px 功能轨。** 活动、联系人、模板、历史、设置；当前页用浅蓝底色标记。
- **活动/批次列表。** 名称、任务数量和状态数量直接可见，支持搜索、新建、重命名与删除。
- **工作区页签。** 联系人为高密度可编辑表（状态、名字、地区、邮箱、窗口、主题、最近更新）；内容页为模板编辑器；窗口页维护窗口顺序与模板/发件人绑定；草稿页集中待确认与 Gmail 草稿。
- **详情检查器。** 联系人资料可编辑；邮件预览只读可复制；当前操作显示进度、日志与停止入口。
- **命令面板。** Ctrl+K 全局搜索并执行动作；高频动作另有 Ctrl+N / Ctrl+I / Ctrl+Enter / Ctrl+D 快捷键。
- **状态表达。** 表格行、摘要行和状态栏同时使用颜色、文字与数量；失败记录始终带错误文本。

**The Command-First Rule.** 键盘优先操作与命令面板覆盖所有核心动作，但鼠标路径保持同等完整。

## Elevation & Shapes

层次依靠表面色、1px 分隔线和留白建立，几乎不使用投影。控件为 3–4px 圆角的紧凑原生形状；表格行高 30px，工具栏与页签贴近 Windows 原生密度。没有玻璃、渐变装饰或大圆角胶囊。

**The Native Density Rule.** 窗口、导航、输入与反馈遵循 Windows 桌面预期：原生标题栏、清晰焦点、可见 hover/disabled/loading 状态。

## Do's and Don'ts

**Do:**

- 保持 56px 功能轨、活动列表、中央工作区和右侧检查器的四栏结构。
- 让活动名称、任务数量、状态名称和下一步操作同时可见。
- 使用克制浅色界面与单一蓝色主动作。
- 用 Ctrl+K 命令面板和键盘快捷键覆盖核心工作流。
- 为浅色、深色、中英文和 Windows 高 DPI 一起验证新增界面。

**Don't:**

- 不要使用大面积深蓝背景、四步横幅或指标卡堆叠。
- 不要使用玻璃拟态、Bento Grid 或"热度"导向的装饰布局。
- 不要用仅有颜色的点、线或图标传达完成、警告或失败。
- 不要把恢复、删除、撤销等低频命令挤进主操作路径。
- 不要让联系人在活动之间重复录入或丢失所属批次。
